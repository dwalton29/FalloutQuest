plugins {
    id 'com.android.application'
}

android {
    namespace 'com.falloutquest.app'
    compileSdk 35
    ndkVersion '27.0.12077973'

    defaultConfig {
        applicationId 'com.falloutquest.app'
        minSdk 29
        targetSdk 35
        versionCode 1
        versionName '0.0.1'

        ndk {
            abiFilters 'arm64-v8a'
        }

        externalNativeBuild {
            cmake {
                cppFlags '-std=c++17'
            }
        }
    }

    externalNativeBuild {
        cmake {
            path file('src/main/cpp/CMakeLists.txt')
            version '3.22.1'
        }
    }
}
